# SDD-002 Constitution index

The future M2 Executor is governed by its Coordinator authorization, repository `AGENTS.md`, accepted M0/M1 contracts, implementation register, architecture/roadmap, dependency/security policy, and `SDD-002-GOVERNED-SHADOW-CAMPAIGN.md`, in that order.

Hard rules: AgentTeams v1.2.0 remains the runtime; exactly six members and real role separation; Leader orchestration-only; Producer/Auditor separation; PostgreSQL is shared state; schema/digest validation at the adapter boundary; secrets and private prompts excluded from public evidence; Owner review is not an ActionGrant; SHADOW cannot execute public actions; canonical progress is Coordinator-owned.

