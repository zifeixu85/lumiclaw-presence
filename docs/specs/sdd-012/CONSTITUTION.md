# SDD-012 Constitution index

本规格按以下优先级受约束：Coordinator 授权与 exact base/worktree → repository `AGENTS.md` → canonical implementation truth → accepted SDD-007/009/010 contracts → `SDD-012-XHS-MEDIA-ARTIFACT-INTEGRATION.md`。

硬规则：

- 本任务 docs-only；状态只到 `SPEC_READY`，不写 implementation/migration/UI/API 或 fake evidence；
- PostgreSQL/BlobStore 分别是业务元数据与内容 bytes 真源，临时 provider URL 不是资产；
- provider 只产生无关键标题文字的 raw 背景/插画；exact overlayCopy 由 pinned deterministic local compositor 渲染，只有 final composited digest 可审校、批准和打包；
- 字体/模板/品牌色/Logo 必须有 exact license/digest，并绑定 approved Brand/Knowledge snapshots；raw/final lineage 与 snapshot invalidation 不可省略；
- provider 名不进入 core domain；controlled fake、real Provider Canary 与 Owner UAT 不可互相冒充；
- terminal-only 且 purpose-separated Secret Gates；Secret 不进 browser/Git/log/prompt/trace/PG dump/package/evidence；
- immutable combined revision、Independent Auditor、exact Owner approval、binary package digest 与 no-`PUBLISHED` 边界不可绕过；
- SDD-012 implementation/Owner UAT 是 SDD-011 硬前置；canonical status 仅 Coordinator 可改。
